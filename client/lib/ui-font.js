/**
 * UI font family — matches `var(--font-ui)` in styles.css (Rewir + system fallbacks).
 * Use in canvas ctx.font and inline styles where CSS variables are unavailable.
 */
export const UI_FONT_FAMILY = "'Rewir', system-ui, -apple-system, 'Segoe UI', sans-serif"
