# Server (playout / dev API host). Copy to .env on the machine that runs `npm start`.
HIGHASCG_HEADLESS=true

# Optional: allow UI from extra origins (comma-separated), e.g. operator laptop Vite
# HIGHASCG_CORS_ORIGINS=http://192.168.0.10:3000,http://127.0.0.1:3000
