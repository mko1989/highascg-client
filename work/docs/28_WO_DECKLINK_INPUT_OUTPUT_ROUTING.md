# WO-28 (pointer)

The active **work order** for DeckLink input/output routing lives in the repo as:

**`work/28_WO_DECKLINK_INPUT_OUTPUT_ROUTING.md`**

That file contains the full specification, **implementation status**, and notes on **host IP splash** after reboot (including DeckLink-only outputs and WebSocket clear behaviour).
